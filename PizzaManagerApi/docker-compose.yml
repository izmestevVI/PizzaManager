services:
  db:
    image: postgres:18-alpine
    container_name: pizza_db_cont
    environment:
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: pizza_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql 

volumes:
  postgres_data:
